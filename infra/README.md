# infra — Pulumi 3 + pulumi-gcp 8

Not started. `brew install pulumi`, then `pulumi new gcp-python`.
Declares: Cloud Run, Firestore indexes, Cloud Scheduler jobs, Secret Manager, IAM.
GCP project `ai-powered-479515`, shared with RELAY.
